/** Indexable paths for robots.txt — always clean public URLs. */
export function marketingRobotsAllowPaths(): string[] {
  return ["/", "/product", "/modules", "/pricing", "/security", "/contact", "/download", "/legal/"];
}
